import React from "react";

const Button = () => {
  return (
    <button style={{ padding: "10px", background: "green", color: "white" }}>
      Remote Button (Auth App)
    </button>
  );
};

export default Button;
